# 操作性改善計画（MD横断レビュー）

## 0. 目的
本計画は、既存の仕様/進捗ドキュメント（`AGENTS.md`, `GAME_SPEC.md`, `README.md`, `IMPLEMENTATION_PLAN.md`, `PHASE0-3_REPORT.md`, `DIFFICULTY_IMAGES.md`）を踏まえ、
**モバイル縦画面（特に iPhone Safari）での操作性を段階的に向上**させるための実行計画を定義する。

---

## 1. ドキュメント横断で見えた現状

### 1.1 強み（すでに土台がある）
- 移動・向き・叩く（0.25秒長押し連打）のコアループは Phase 3 時点で実装済み。
- 盤面生成（安全地帯、地雷、ゴール距離制約）は Phase 2 時点で検証済み。
- MVP範囲は「探索パズル」に収束しており、スコープ逸脱抑制が効いている。

### 1.2 操作性観点の主要ギャップ
- **向きの視認性が背景によって埋もれる可能性**（仕様上の要求はあるが、最終最適化は未完了）。
- **長押し時の入力感（誤爆/遅延/連打開始タイミング）**の調整指針が未整理。
- **片手操作を前提としたUIヒット領域**（下部20%ルール内での指届き最適化）の定量基準が未定義。
- **十字キー入力と叩く入力の同時押し**（独立した押下状態管理）が計画上未定義。
- **勝敗遷移時の入力制御**（誤入力防止、次アクション誘導）が未最終化。
- **ヘルプ内容はあるが、初見時の行動誘導が弱い**（"向きだけ変えられる"等の重要概念が体験導線化されていない）。

---

## 2. 操作性改善の優先施策（MVP準拠）

## P0（最優先: 直近1スプリント）

### P0-1. プレイヤー向き表示の高コントラスト化
- 施策:
  - 向き矢印にアウトライン/シャドウを追加。
  - 背景タイル（床/焼け跡/スタート）ごとの最低コントラスト比を確保。
  - 必要に応じて矢印の太さとサイズを段階的に調整可能な定数化。
- 期待効果:
  - 「今どこを叩くか」が即時に分かり、誤タップが減る。

### P0-2. 長押し入力の体感改善（移動/叩く）
- 施策:
  - 連打間隔（現状250ms）は維持しつつ、
    - 押下直後の初回反応（即時1回）
    - ホールド継続時の等間隔反復
    を明確に分離。
  - 連打中の視覚フィードバック（ボタン押下状態）を強化。
- 期待効果:
  - 「押しているのに反応しない/急に連打される」感覚を抑制。

### P0-3. 入力ボタンのヒット領域拡張（見た目据え置き可）
- 施策:
  - 十字キーと叩くボタンの当たり判定を視覚サイズより大きく設定。
  - iPhone縦持ちで親指到達しやすい位置へ微調整。
- 期待効果:
  - タップミスを低減し、疲労を軽減。

### P0-4. 十字キーと叩くボタンの同時押し対応
- 施策:
  - 移動入力系（十字キー）と叩き入力系（叩く）を別チャネルで管理し、同一フレーム内で両方有効化。
  - ポインターID単位で入力状態を保持し、多点タッチ時に片方の入力が他方を上書きしないようにする。
  - 同時押し中も各リピート間隔（250ms）を独立維持する。
- 期待効果:
  - 片手/両手のどちらでも「移動しながら掘る」操作が安定し、テンポが向上する。

### P0-5. 十字キーのスライド入力対応（指を離さず方向変更）
- 施策:
  - 十字キー押下中のポインターIDを追跡し、`pointerover` で別方向ボタンへ入った時に入力所有権を移譲する。
  - 「押し始めた方向のリピート」を停止して「新しい方向の即時1回 + 250ms反復」を開始する。
  - 指が画面に触れたままなら入力継続、指を離した時のみ停止する。
- 期待効果:
  - 「→を押したまま↑へスライド」のような連続方向転換が自然に行え、モバイル特有の操作負荷を下げられる。

---

## P1（次点: P0完了後）

### P1-1. 勝利/敗北時の入力ロックと導線整理
- 施策:
  - 勝敗確定後は移動/叩き入力を停止。
  - 画面下部に「再スタート」への主導線を明示。
- 期待効果:
  - 状態遷移時の混乱防止。

### P1-2. ヘルプモーダルの文面最適化（短文化 + 行動優先）
- 施策:
  - 1画面で読める短文化。
  - 優先順を「移動→向き→叩く→数字→ゴール」に統一。
  - "移動できなくても向きが変わる"を先頭近くへ配置。
- 期待効果:
  - 初見プレイヤーの理解コスト低下。

### P1-3. 盤面と操作領域の境界明確化
- 施策:
  - 盤面枠と操作領域の視覚分離を強化。
  - 誤タップで盤面に干渉しないレイヤー構成を再確認。
- 期待効果:
  - 操作意図の取り違えを減らす。

---

## P2（余力対応）

### P2-1. マイクロフィードバック追加
- 施策:
  - 叩き成功/地雷命中/移動不可の軽い視覚反応（揺れ/点滅）を最小追加。
- 期待効果:
  - ルール理解の定着を促進。

### P2-2. 難易度別のUIスケール最適化
- 施策:
  - 16x16 / 30x16 で文字サイズ・線幅の下限を再調整。
- 期待効果:
  - EXPERTでの可読性維持。

---

## 3. 実装タスク分解（推奨モジュール境界に準拠）

1. `src/game/constants.ts`（または同等）
   - 入力反復間隔、押下初回遅延、UIヒット余白、矢印表示定数を集約。
2. `src/game/input/*`
   - 押下直後1回 + ホールド反復の共通制御化。
   - 移動系入力と叩き系入力の同時押し管理（ポインターID追跡）を追加。
3. `src/game/ui/*`
   - 十字キー/叩くボタンの当たり判定拡張。
   - ボタン押下状態表示の一貫化。
4. `src/game/render/*`
   - 向き矢印の高コントラスト化（縁取り・影）。
5. `src/game/GameScene.ts`
   - 勝敗後の入力ロック。
   - ヘルプ導線と再スタート導線の最終統合。

---

## 4. 受け入れ基準（操作性）

- iPhone Safari 縦画面で以下を満たすこと:
  1. 10回連続で、狙った方向へ向き変更できる（誤方向0回）。
  2. 30秒連続の長押し移動で、入力抜け/暴発が体感上ほぼない。
  3. 叩く長押しで、反復タイミングが一定に感じられる。
4. 焼け跡上・床上・スタート上のいずれでも向き矢印が即視認できる。
5. 勝敗表示中に移動/叩きが反応しない。
6. 十字キー長押し中に叩く長押しを開始しても、両入力が同時に継続する。
  7. 十字キー長押し中に別方向へ指をスライドしたら、指を離さず新方向入力へ切り替わる。

---

## 5. 検証計画

- ビルド検証:
  - `npm run build`
- 端末検証（最低）:
  - iPhone Safari（SE系画面高・標準サイズ画面高）
- 手動操作チェックリスト:
  - 片手親指で十字キー4方向を連続入力。
  - 叩く長押しで地雷/安全混在ケースを確認。
  - 十字キー長押し + 叩く長押しの同時押しを10秒以上維持し、どちらも途切れないことを確認。
  - 十字キーの1方向長押し中に、指を離さず別方向へ2回以上スライドし、毎回方向転換できることを確認。
  - 勝利/敗北遷移時の入力ロック確認。

---

## 6. 実行順序（提案）

1. P0-1（向き視認性）
2. P0-3（ヒット領域）
3. P0-4（同時押し対応）
4. P0-5（スライド入力対応）
5. P0-2（長押し体感）
6. P1-1（勝敗入力ロック）
7. P1-2（ヘルプ最適化）
8. P1-3（境界明確化）
9. P2群（必要時）

この順序は、**誤操作削減 → 連続操作の安定化 → 状態遷移の迷い排除** の順に効果が高いため。
