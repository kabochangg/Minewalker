# GAME_SPEC.md

## プロジェクト名
Minewalker

## 目的
プレイヤーがキャラクターを操作し、マインスイーパー風の数字を読み、壁を壊し、地雷を避けながら隠されたゴールを探す、モバイル向けブラウザゲームを構築・改善する。

このプロジェクトは、従来の盤面全体を解くマインスイーパーではない。
これは、**移動を伴う探索型パズルゲーム**であり、マインスイーパーから着想を得ている。

## 現在のプロダクト方針
現在の優先順位は、**探索 / マインスイーパーの中核ループ**を先に作ること。

明示的な指示がない限り、以下を追加しないこと:
- RPGシステム
- バトルシステム
- インベントリシステム
- 敵
- マルチプレイ
- 成長システム

現在の設計目標:
- 十字キーでキャラクターを動かす
- 向きを持つ
- キャラクター正面の壁を壊す
- 安全なら新しい床が開く
- 地雷ならダメージを受け、通行可能な焼け跡マスになる
- 数字を読み、安全なルートを推理する
- 隠されたゴールを見つけて到達する

## コアゲームルール

### 盤面サイズ
標準的なマインスイーパー相当のサイズを使う:
- 中級: 16x16、地雷40
- 上級: 30x16、地雷99

明示的な指示がない限り、マス数を変更しないこと。

### マスの種類
盤面には以下のマスが存在する:
- 壁
- 開いた床
- 焼け跡マス
- スタート領域
- 隠しゴールマス

### 開いた床
- 通行可能
- 周囲8マスの地雷数を表示する
- 地雷数が0なら表示を省略してよい

### 焼け跡マス
- 地雷入りの壁を叩いた結果できる
- 通行可能
- 通常床と見た目で区別すること
- 暗い「焼け跡」色を使うこと
- 周囲8マスの地雷数を表示する
- 地雷数が0なら表示を省略してよい

### ゴールマス
- 最初は隠れている
- そのマスが開いた時だけ見える
- 数字は表示しない
- 到達するとステージクリア

### 壁
- 未開封
- 通行不可
- プレイヤー正面にある時だけ壊せる

## プレイヤールール

### 移動
- 移動は画面上の十字キーを使う
- 1入力で1マス移動
- MVPでは即時移動でよい
- 移動先が塞がれていれば移動しない
- 移動できなくても向きは変わる

### 向き
プレイヤーの向きは4方向のみ:
- 上
- 下
- 左
- 右

### 叩く / 壁破壊
- 「叩く」ボタンで、プレイヤー正面1マスの壁を壊す
- 対象は必ず1マスのみ
- 4方向のみ
- 斜めは不可

### 長押し連打
叩くボタンは押しっぱなし連打を許可する。
- 連打間隔: 0.25秒 / 打
- 将来調整できる定数として管理すること

## 地雷との相互作用
正面の壁が安全なら:
- 開いた床になる
- 周囲地雷数を表示する
- 通行可能になる

正面の壁に地雷があるなら:
- HPを1失う
- そのマスは焼け跡マスになる
- 焼け跡マスは通行可能
- 焼け跡マスにも周囲地雷数を表示する
- プレイヤーは視覚的に「ノックバック」する
- 実装上は元の位置に残る形でもよいが、反動を受けた感覚を出すこと

## HP
- 初期HP: 3
- 地雷入りの壁を叩くたびにHP-1
- HPが0でゲームオーバー

## 隠しゴールルール
- ゴールは最初から隠れている
- 閉じたマスの中に埋まっている
- そのマスが開いた時だけ見える
- ゴールは初期安全地帯の外側に置く
- さらにスタート周辺18x18相当の領域の外側に置く

## 初期安全地帯
ゲームは、比較的広い安全な洞窟の中から始まる。

要件:
- 1つの連結領域であること
- 飛び地なし
- ランダム形状であること
- 固定長方形ではないこと
- 細い通路も許可する
- 分岐数は1〜4
- 形状は不規則でよい
- 面積目標は約81マス
- 許容範囲は66〜96マス
- この領域内に地雷は置かない
- スタート位置はこの安全領域内であること

## 地雷
地雷数は、選択された盤面サイズに応じて標準的なマインスイーパーと同じ数を使う:
- 16x16 -> 40
- 30x16 -> 99

地雷は初期安全地帯の外に配置する。

## 数字
数字は標準的なマインスイーパーのルールに従う:
- 周囲8方向の地雷数を数える
- 開いた床に表示する
- 焼け跡マスにも表示する
- ゴールマスには表示しない
- 0は空表示でよい
- MVPでは0の自動展開はしない

## UIの優先順位
このプロジェクトはモバイルファーストであり、特に iPhone Safari を重視する。

優先順位:
1. 盤面の見やすさ
2. 縦画面で安定したレイアウト
3. わかりやすい十字キーと叩くボタン
4. 速い反応
5. 余計な情報を減らすこと

### HUD
HUDは最小限にする:
- HP
- ヘルプボタン
- 必要ならリスタートボタン

### ヘルプ
説明を常時表示しないこと。
`?` ボタンでヘルプを表示すること。

ヘルプ内容:
- 十字キーで移動
- 移動できなくても向きは変わる
- 叩くと正面の壁を壊す
- 数字は周囲地雷数
- 地雷はダメージになる
- 焼け跡マスは通れる
- ゴールは開くまで見えない

## ビジュアル方針
装飾性より、見やすくゲームらしい見た目を優先する。

重要:
- 盤面を主役にする
- 画面情報を増やしすぎない
- 焼け跡マスは通常床より暗くする
- ゴールは出現後に明確に区別できるようにする
- 文字や記号はモバイルでもシャープに見えること

## 技術方針
現在のスタック:
- Phaser 3
- TypeScript
- Vite

明示的な指示がない限り、このスタックを維持する。

## アーキテクチャ指針
小さく理解しやすいモジュールを優先する。
可能な範囲で責務を分ける:
- 盤面生成
- タイル状態 / データモデル
- プレイヤー移動と向き
- 叩く / 壁破壊処理
- 地雷 / ダメージ解決
- ゴール出現 / 勝利条件
- UI操作とHUD
- モバイルレイアウト処理

明確な必要がない限り、大きな先回りリファクタをしないこと。

## 指示がない限り追加しないもの
追加しない:
- HP以外のRPGステータス
- Ore
- 回復アイテム
- ピッケル強化
- 敵
- マルチプレイ
- タイマー
- flagモード
- 0の自動展開
- 複雑なストーリーシステム
- 重いアニメーションシステム

## 実装スタイル
変更時は:
- 小さく狙いを絞った変更を優先
- 無関係なリファクタを避ける
- 定数は調整可能に保つ
- モバイルの安定性を優先
- buildが通る状態を維持

常に満たすこと:
- `npm run build` が通る
- モバイル縦画面で遊べる
- 既存の意図したゲームメカニクスを誤って消さない

## 納品時の期待
作業完了時は:
- 何を変えたか要約する
- 変更ファイルを列挙する
- 前提や仮定を書く
- リスクや限界があれば書く
- 依頼範囲に収まるようにする