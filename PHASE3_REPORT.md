# Phase 3 実施記録（プレイヤー操作とコアループ）

## 1. 着手前チェック（AGENTS.md再確認）
- [x] MVP範囲外を追加しない
- [x] 盤面サイズ（16x16/40, 30x16/99）を変更しない
- [x] モバイル縦画面優先のUI方針を守る
- [x] `npm run build` を通す

## 2. 実装内容
- `src/game/GameScene.ts` を Minewalker のコアループに合わせて再構成。
  - 十字キー入力で 1入力1マス移動を実装。
  - 移動不可でも向きのみ更新する仕様を実装。
  - 叩く操作を「プレイヤー正面1マスのみ」に固定。
  - 叩く長押し連打を `250ms` 定数間隔で実装。
  - 安全壁を開いたときに床化し、数字表示する処理を実装。
  - 地雷壁を叩いたときに HP-1、焼け跡化、数字表示、前進しない挙動を実装。
- UIをMVP方針に寄せて更新。
  - 上部HUDに HP / ステータス / `?` / 再スタートを配置。
  - 下部操作に十字キーと「叩く」ボタンを配置。
  - ヘルプモーダル文面を Minewalker 操作説明に更新。

## 3. エラー箇所修正
- 既存の「マインスイーパー開封ループ（flag/chord/0自動展開）」が Phase 3 要件と衝突していたため、
  Minewalker の移動・向き・叩き中心のループへ差し替え。
- 叩く連打間隔が仕様（0.25秒）と一致していなかったため、`250ms` 定数に修正。

## 4. MVP範囲の明確化（評価）

### 4.1 Phase 3 の評価対象（今回評価する項目）
- [x] 操作可能なプレイヤーキャラクター
- [x] 十字キーによる4方向移動
- [x] 4方向の向き管理（移動失敗時も向き更新）
- [x] 正面1マスのみを対象にする叩く処理
- [x] 叩く長押し連打（250ms間隔）
- [x] 安全壁を開いた際の床化と数字表示
- [x] 地雷壁を開いた際のHP減少・焼け跡化・数字表示・前進しない挙動

### 4.2 Phase 3 の評価対象外（後続フェーズ）
- [ ] HP=0時の敗北遷移の最終UI調整（Phase 4で仕上げ）
- [ ] ゴール到達時の勝利導線の最終調整（Phase 4で仕上げ）
- [ ] モバイルUI視認性の最終最適化（Phase 5）

## 5. 評価結果（Phase 3）
- 判定: **Pass（フェーズ3目標を満たす）**
- 根拠:
  1. 入力の中心が「移動・向き・叩く」に統一され、探索ループとして成立。
  2. 叩くは正面1マス限定で、仕様外の斜め処理は存在しない。
  3. 地雷叩き時にHP減少と焼け跡化が発生し、ノックバック要件（前進しない）を満たす。

## 6. 検証結果
- `npm run build` 成功。

## 7. 未解決課題
- 勝利/敗北時の演出・入力制御の最終整理は Phase 4 で継続。

## 8. AGENTS.md更新要否
- 現時点では不要。
- ノックバックの「表現」基準（SE・画面揺れ等）を運用ルール化する場合は、Phase 4以降で更新提案可能。
