# IMPLEMENTATION_PLAN.md

## 目的
AGENTS.md と GAME_SPEC.md の要件を満たす Minewalker MVP を、スコープを広げず段階的に実装するための計画書。

## 前提
- 技術スタックは Phaser 3 / TypeScript / Vite を維持する。
- 対象はモバイル縦画面（特に iPhone Safari）を最優先とする。
- デプロイ先は Cloudflare Pages を前提にする。
- 各フェーズ完了時に `npm run build` が通る状態を維持する。

## フェーズ運用ルール（全フェーズ共通）
1. **着手前チェック（必須）**
   - フェーズ開始前に `AGENTS.md` を再読し、以下の4点をチェックリストで確認する。
     - MVP範囲外を追加しない。
     - 盤面サイズ（16x16/40, 30x16/99）を変更しない。
     - モバイル縦画面優先のUI方針を守る。
     - `npm run build` を通す。
2. **実装中の仕様同期（必要時）**
   - 次のいずれかが発生した場合は、実装中に `AGENTS.md` 更新を提案し、合意後に更新する。
     - 仕様の解釈が複数成立し、実装方針が一意に決められない。
     - 新しい定数（例: 連打間隔、最低タップ領域）を導入し、運用ルール化が必要。
     - 実機検証で既存記述では不足する制約が判明。
3. **完了時確認（必須）**
   - フェーズ終了時に「実装内容」「未解決課題」「AGENTS.md更新要否」を記録する。

## 実装方針（全体）
1. 先にゲームループ成立（移動・向き・叩く・被ダメージ・勝敗）を優先する。
2. 次に盤面品質（安全地帯・ゴール距離・数字整合）を固める。
3. 最後にモバイルUIの視認性と操作性を仕上げる。
4. 変更は1テーマずつの小さい差分で進め、無関係なリファクタをしない。

---

## フェーズ0: 現状把握と固定
### 開始前
- `AGENTS.md` を確認し、スコープ外機能を入れないことを確認する。

### 目標
- 既存コードの責務を明確化し、以降の変更対象を固定する。

### タスク
- `GameScene` の処理を「盤面生成 / 入力 / 描画 / 勝敗遷移」に分類して一覧化する。
- 既存定数を抽出し、未定数化項目を列挙する（例: 叩く連打間隔、UI余白）。
- MVP範囲外要素の混入有無を確認し、見つかった場合は削除候補として記録する。

### AGENTS.md更新条件
- 現行コードに対して仕様本文の不足（例: 判定優先順位の欠落）を見つけた場合に更新提案。

### 完了条件
- 変更対象ファイル一覧と、各ファイルで変更する責務が1行で説明できる。

---

## フェーズ1: データモデルの明確化
### 開始前
- `AGENTS.md` を確認し、盤面サイズ固定・MVP範囲固定を再確認する。

### 目標
- 壁/床/焼け跡/ゴール/スタートを型安全に扱える土台を作る。

### タスク
- タイル状態型を定義する（`tileKind`, `isOpen`, `hasMine`, `adjacentMineCount`, `goalState`）。
- 方向型を4方向で定義し、座標加算ユーティリティを用意する。
- 盤面設定定数を分離する（`INTERMEDIATE: 16x16/40`, `EXPERT: 30x16/99`）。

### AGENTS.md更新条件
- 新しい共通定数の管理方針（命名・配置規約）をチームルール化する必要が出た場合に更新提案。

### 完了条件
- 盤面ロジックで文字列リテラル分岐を使わず、型経由で状態判定できる。

---

## フェーズ2: 盤面生成（MVP要件準拠）
### 開始前
- `AGENTS.md` を確認し、安全地帯要件（連結・66〜96・不規則）を再確認する。

### 目標
- 初期安全地帯・地雷・隠しゴールを仕様通りに生成する。

### タスク
- 安全地帯を生成する。
  - 面積: 66〜96（目標81）
  - 連結: BFS/DFSで単一連結を検証
  - 形状: 固定長方形禁止（幅・高さが常に一定になる生成は不可）
- 地雷を配置する。
  - 安全地帯外の閉鎖マスのみ対象
  - 総数は難易度固定（40 or 99）
- ゴールを配置する。
  - 安全地帯外
  - スタート中心18x18相当の禁止領域外
- 各マスの周囲8マス地雷数を前計算する。

### AGENTS.md更新条件
- 18x18禁止領域の境界解釈（端数処理など）を実装で固定した場合に明文化提案。

### 完了条件
- 100回生成しても、地雷数・安全地帯面積・連結性・ゴール距離制約違反が0件。

---

## フェーズ3: プレイヤー操作とコアループ
### 開始前
- `AGENTS.md` を確認し、移動/向き/叩く/長押し仕様を再確認する。

### 目標
- 「移動」「向き変更」「叩く」で探索ループを成立させる。

### タスク
- 十字キー入力で1入力1マス移動を実装（失敗時は位置維持、向きのみ更新）。
- 叩くで正面1マスのみ処理（斜め不可）。
- 長押し連打を `250ms` 間隔定数で実装。
- 安全壁: 開いた床化 + 数字表示。
- 地雷壁: HP-1 + 焼け跡化 + 数字表示 + 前進しないノックバック表現。

### AGENTS.md更新条件
- ノックバック「表現」の最小要件（SE/揺れ/フラッシュ等）を運用基準化する必要が出た場合に更新提案。

### 完了条件
- 10分の手動プレイで、操作不能・意図しない連打停止・誤方向破壊が再現しない。

---

## フェーズ4: 勝敗条件
### 開始前
- `AGENTS.md` を確認し、HP/敗北/ゴール表示条件を再確認する。

### 目標
- 勝利/敗北の状態遷移を一意にする。

### タスク
- 初期HP=3をHUD表示。
- HP=0で敗北状態（入力停止 + リスタート導線表示）。
- ゴールマスを開いたときのみ可視化。
- 可視化後ゴール到達で勝利状態へ遷移。

### AGENTS.md更新条件
- 敗北/勝利時UIの最小表示要件を明文化する必要が出た場合に更新提案。

### 完了条件
- 勝利・敗北それぞれで、遷移後に入力受付状態が仕様どおり（継続/停止）で固定される。

---

## フェーズ5: モバイルUI最適化
### 開始前
- `AGENTS.md` を確認し、HUD最小構成と盤面主役方針を再確認する。

### 目標
- iPhone Safari縦画面で視認性と操作性を担保する。

### タスク
- 上部HUDを `HP / ? / (必要時)Restart` の最小構成にする。
- 下部操作領域に十字キーと叩くボタンを固定配置。
- 数字・焼け跡・ゴールを判別できる配色コントラストを確保。
- `?` からヘルプモーダルを開閉し、指定された説明項目のみ表示。
- タップ領域最小サイズを `44px` 以上で確保（iPhone操作性対策）。

### AGENTS.md更新条件
- 実機検証で最小タップサイズや余白基準をルール化すべきと判断した場合に更新提案。

### 完了条件
- iPhone縦画面相当で、盤面が常時視認でき、主要操作を片手で実行可能。

---

## フェーズ6: 品質確認とデプロイ準備
### 開始前
- `AGENTS.md` を確認し、build通過とCloudflare Pages前提を再確認する。

### 目標
- ビルド安定性と配信前提を満たして仕上げる。

### タスク
- `npm run build` を実行し成功させる。
- 手動チェック項目を実施する。
  - 移動
  - 向き変更
  - 叩く（安全/地雷）
  - HP減少と敗北
  - 焼け跡通行
  - ゴール開示と勝利
- Viteの出力がCloudflare Pages配信構成で問題ないことを確認する。

### AGENTS.md更新条件
- リリース運用で恒常的に必要な検証手順が増えた場合に更新提案。

### 完了条件
- build成功 + チェックリスト全項目Pass + 既知課題が文書化済み。

---

## 優先度付き実装バックログ
### P0（最優先）
- プレイヤー移動/向き
- 叩く処理（安全/地雷）
- HP/敗北
- 隠しゴール/勝利

### P1（次点）
- 安全地帯生成品質（連結・面積）
- ゴール遠距離制約
- モバイルUI配置の改善

### P2（仕上げ）
- 色・文字サイズ・余白の調整
- ヘルプ文面の最適化
- 内部定数の調整容易性向上

## リスクと対策
- **リスク1: 安全地帯生成が不安定**
  - 対策: 最大リトライ回数（例: 50）を定義し、失敗時は再生成する。
- **リスク2: モバイルで操作誤爆**
  - 対策: ボタン最小サイズ44px、ボタン間隔8px以上を定数管理する。
- **リスク3: 1ファイル肥大化**
  - 対策: 盤面生成・入力・叩く解決を別モジュールへ分離し、1ファイル300行超で分割検討。

## 完了の定義（Definition of Done）
- MVP範囲内で、探索パズルとして1プレイが完結する。
- 盤面サイズと地雷数が仕様どおり固定である。
- 地雷・焼け跡・数字・ゴール表示が仕様どおり動作する。
- モバイル縦画面での操作と視認性が実用レベルである。
- `npm run build` が成功する。
