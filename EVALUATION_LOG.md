# EVALUATION_LOG.md

## 1. 目的
このファイルは、Minewalker の評価観点・評価結果・作業ログを 1 か所に集約して記録する。
今後の検証追加もこのファイルへ追記する。

## 2. 共通評価観点
### 仕様適合
- MVP 範囲外の機能を追加していないか
- 盤面サイズ / 地雷数 / 安全地帯 / ゴール条件が仕様どおりか
- モンスター出現条件 / 追跡挙動 / 勝利 / 敗北条件が仕様どおりか

### 操作性
- モバイル縦画面で操作しやすいか
- 向き表示が見やすいか
- 長押し / 同時押し / スライド操作が安定するか

### 技術品質
- `npm run build` が成功するか
- 型エラーや配信上の問題がないか
- Cloudflare Pages 前提の構成を維持できているか

## 3. フェーズ別サマリー
### Phase 0: 現状把握
- `GameScene` の責務を 盤面生成 / 入力 / 描画 / 勝敗遷移 に分類した。
- 既存定数と未定数化項目を洗い出した。
- 旧来のマインスイーパー寄り機能が Minewalker MVP とずれていることを確認した。
- 判定: Pass

### Phase 1: データモデル明確化
- タイル状態、方向、盤面設定定数を分離した。
- 文字列リテラル依存を減らし、型経由の判定へ移行した。
- 判定: Pass

### Phase 2: 盤面生成
- 安全地帯、地雷、ゴール、隣接地雷数の生成責務を整理した。
- 100 回生成検証で制約違反 0 件を確認した。
- 判定: Pass

### Phase 3: コアループ
- 移動、向き変更、叩く、地雷位置の開示、勝敗遷移を実装した。
- UI を Minewalker 向けに更新した。
- 判定: Pass

### 操作性改善対応
- 勝敗時入力ロック
- モンスター接触敗北への置換準備
- ヘルプ短文化
- 盤面と操作領域の境界強化
- 0 タイル連鎖開示対応
- 判定: Pass

## 4. 自由移動化の実装計画
### 方針
- 盤面データはグリッドのまま維持し、プレイヤーとモンスターの位置・入力解釈を連続座標ベースで扱う。
- 変更は主に `src/game/GameScene.ts` に集約し、盤面生成や定数定義への影響は最小限に留める。
- モバイル縦画面での操作性を優先し、自由移動・斜め移動・正面判定ボックスによる叩きが安定することを重視する。

### 実装ステップ
1. プレイヤー位置をセル座標ではなくワールド座標で持つように変更し、半径が 1 セルの約 60% となる円形ヒットボックスを導入する。
2. 十字キー入力を「1 回ごとの移動」から「押下中の移動ベクトル管理」へ切り替え、複数同時押しで斜め移動できるようにする。
3. `update()` ベースで移動処理を行い、プレイヤー移動速度をまず 1 秒あたり約 2.5 セル分に設定する。
4. 地雷位置を叩いた時に対象マスを開き、そこへモンスター 1 体を生成する。
5. モンスターもプレイヤーと同じ連続移動ロジックを使い、速度はプレイヤー比約 80% に設定する。
6. 未開封の壁マスとの衝突判定を追加し、開いている床 / モンスター出現マス / 開示済みゴールの空間内だけを自由に移動できるようにする。
7. プレイヤー向きを入力方向ベースの 8 方向へ拡張し、移動できない場合でも向きだけは更新する。
8. 叩き処理を隣接 1 マス指定から正面判定ボックス方式へ変更し、プレイヤー前面 0.6〜0.9 セル先の短いボックス内にある未開封マスだけを対象にする。
9. 判定ボックス内に複数候補が入る場合は、最も近い 1 枚だけを開ける優先ロジックを追加する。
10. プレイヤーとモンスターの接触判定を追加し、接触時は即敗北に遷移させる。
11. ゴール判定を「ゴールマスへグリッド移動した時」から「開示済みゴール領域へ侵入した時」へ変更する。
12. 長押し移動、斜め移動、壁密着時の叩き、モンスター追従、接触敗北、勝敗時入力ロックを手動確認し、最後に `npm run build` を実行する。

### 検証観点
- 斜め移動時に壁角へ過度に引っかからないこと。
- 壁に密着した状態でも、狙った正面 1 マスだけを安定して叩けること。
- 十字キー長押し中でも叩き入力が独立して継続すること。
- モンスターがプレイヤー比約 80% の速度で連続移動し、視認できること。
- プレイヤーとモンスターが接触した時点で敗北に遷移し、その後は入力が無効になること。
- 開示済みゴール領域へ入った時点で勝利に遷移し、その後は入力が無効になること。

## 5. 実行ログ
### 2026-03-18
#### 実施内容
- モンスター出現仕様へのドキュメント更新を実施
- 変更後の build を実施

#### 評価結果
1. `npm run build`
   - 成功

#### 補足
- 今回は仕様更新のみで、コード実装や画面アーティファクト更新は未実施

## 6. 今後の記録ルール
- 新しい評価観点はまず「共通評価観点」に追記する
- 実施結果は日付単位で「実行ログ」に追記する
- フェーズ単位の要約が必要な場合は「フェーズ別サマリー」を更新する

### 2026-03-20
#### 実施内容
- 地雷壁を叩いた時にモンスターが出現し、プレイヤーへ連続追跡する実装を追加
- プレイヤー移動速度を 2.75 cells/sec に微調整
- プレイヤー座標をセル中心基準へ統一し、壁へのめり込みと過剰な近接ブロックを同時に緩和
- 画面アーティファクトを更新し、モンスター表示を反映

#### 評価結果
1. `npm run build`
   - 成功
   - Vite の chunk size 警告は出たが、ビルド自体は完了
2. `npm run render:gamescreen`
   - 成功
   - `artifacts/game-screen.svg` を更新

#### 補足
- 自動評価では型エラー・ビルドエラーは再現せず解消済み
- 操作感の最終確認は実機ブラウザでの追試が望ましいが、今回の修正対象である座標系ずれと衝突半径はコード上で整合を取った

### 2026-03-22
#### 実施内容
- 操作パッド操作性改善の事前ドキュメント更新を実施
- 上部 HUD を現状比およそ 60% へ圧縮する方針を仕様へ追記
- 下部操作領域を現状比 1.2 倍へ拡張し、操作パッドを大型化して中央寄せする方針を追記

#### 評価結果
1. `npm run build`
   - 成功
   - Vite の chunk size 警告は出たが、ビルド自体は完了

#### 補足
- 今回は仕様整理フェーズのため、コード実装・画面アーティファクト更新は未実施
