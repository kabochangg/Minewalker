# skills.md

## 目的

Minewalker リポジトリで AI エージェントが作業する際に意識するスキルと役割を整理します。
このファイルは作業補助用のメモであり、正式な仕様は `GAME_SPEC.md`、操作性要件は `USABILITY.md` を優先します。

## 必要なスキル

### TypeScript / Vite

- 型エラーを出さない実装を行う。
- `npm run build` で `tsc --noEmit` と Vite ビルドが通る状態を維持する。

### Phaser 3

- `src/game/GameScene.ts` を中心に、シーン、入力、描画、タイマー、当たり判定を扱う。
- モバイル端末での描画負荷と操作反応を意識する。

### モバイル UI / UX

- iPhone Safari の縦画面を前提に、盤面を主役にしたレイアウトを保つ。
- 移動パッド、ヘルプ、リスタート、次ステージ導線の押しやすさを確認する。

### ゲーム仕様管理

- `GAME_SPEC.md` と実装の食い違いを増やさない。
- 数値調整をした場合は `USABILITY.md` と `EVALUATION_LOG.md` も確認する。

### 検証と記録

- 変更後は `npm run build` を実行する。
- 操作性や視認性に関わる変更は、可能な範囲で評価観点を `EVALUATION_LOG.md` に残す。

## 作業姿勢

- 小さな変更を積み重ねる。
- 責務分割と定数化を優先する。
- 無関係な大規模リファクタを避ける。
- 仕様外の新機能を追加しない。
