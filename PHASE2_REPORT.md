# Phase 2 実施記録（盤面生成・MVP要件準拠）

## 1. 着手前チェック（AGENTS.md再確認）
- [x] MVP範囲外を追加しない
- [x] 盤面サイズ（16x16/40, 30x16/99）を変更しない
- [x] モバイル縦画面優先のUI方針を守る
- [x] `npm run build` を通す

## 2. 実装内容
- `src/game/boardGenerator.ts` を追加し、盤面生成責務を `GameScene` から分離。
  - 安全地帯生成（連結・面積66〜96、目標81）
  - 地雷配置（安全地帯外・難易度固定数）
  - ゴール配置（安全地帯外・スタート近傍18x18相当禁止領域外）
  - 周囲8マス地雷数の前計算
- 形状品質担保として、安全地帯が「充填長方形」になるケースを除外。
- `validateGeneratedBoard` を実装し、以下を検証可能化。
  - 安全地帯面積
  - 安全地帯の単一連結性
  - 安全地帯の不規則性（充填長方形除外）
  - 地雷総数
  - 地雷の安全地帯外配置
  - ゴール制約（地雷でない・hidden・禁止領域外）
  - 隣接地雷数の整合
- `GameScene` は `generateBoard(...)` を使用するよう変更し、初期盤面生成を新モジュールへ移譲。

## 3. エラー箇所修正
- 生成品質の評価関数に不足していた検証を追加。
  - 不規則性チェックが検証関数側で漏れていた問題を修正。
  - スタート/ゴールタイルの状態整合（`START` / `HIDDEN`）検証を追加。
  - 地雷タイルの整合（open禁止・goalState禁止）検証を追加。

## 4. MVP範囲の明確化（評価用）
### 4.1 Phase 2 の評価対象（今回評価する項目）
- [x] 初期安全地帯が連結であること。
- [x] 初期安全地帯面積が66〜96に収まること。
- [x] 初期安全地帯が固定長方形の充填形ではないこと。
- [x] 地雷が安全地帯外にのみ配置されること。
- [x] 地雷総数が難易度定数（40 / 99）と一致すること。
- [x] ゴールが安全地帯外かつスタート近傍18x18相当禁止領域外であること。
- [x] 周囲8マス地雷数が全マスで整合すること。

### 4.2 Phase 2 の評価対象外（後続フェーズで評価する項目）
- [ ] プレイヤー移動/向き/叩く（Phase 3）
- [ ] HP/敗北/ゴール到達による勝利遷移（Phase 4）
- [ ] モバイルUI最適化（Phase 5）

## 5. 評価結果（Phase 2）
- 判定: **Pass（フェーズ2目標を満たす）**
- 根拠:
  1. `generateBoard` が安全地帯・地雷・ゴール・隣接地雷数を一括生成。
  2. `validateGeneratedBoard` で面積/連結/不規則性/地雷数/ゴール制約/数字整合を検証可能。
  3. 100回生成検証（各難易度）で制約違反0件を確認。

## 6. 検証結果
- 100回生成（各難易度）で、制約違反は 0 件。
- `npm run build` 成功。

## 7. 未解決課題
- Phase 3 以降で実装予定の「移動/向き/叩く」中心ループへの置換は未着手。

## 8. AGENTS.md更新要否
- 現時点では不要。
- 18x18禁止領域は `dx:[-9,8], dy:[-9,8]` として実装したため、運用上の認識合わせが必要なら後続で明文化を提案可能。
