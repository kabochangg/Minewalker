# AGENTS.md

# Minewalker 仕様書

## 1. 技術要件

### 1.1 スタック
使用技術:
- Phaser 3
- TypeScript
- Vite

### 1.2 build要件
変更後も次が通ること:
- `npm run build`

### 1.3 対象プラットフォーム
主対象:
- iPhone Safari
- モバイル縦画面

### 1.4 デプロイ先
デプロイ先として **Cloudflare Pages** を使用する。

前提:
- URL公開前提で動作すること
- 単一HTML直開きは前提にしない
- Cloudflare Pages 上で問題なく配信できる構成を維持すること

---

## 2. 実装制約

### 2.1 スコープ管理
この仕様にない無関係な機能を勝手に足さないこと。

### 2.2 リファクタ指針
優先すること:
- 小さく狙いを絞った変更
- 明確なモジュール分割
- 定数の外出し

避けること:
- 大きすぎる先回りリファクタ
- スコープの拡大
- 早い段階でのRPG化や戦闘化

### 2.3 実装時の基本方針
- モバイル縦画面での安定動作を優先する
- 盤面の見やすさを最優先する
- iPhone Safari での操作性を重視する
- Cloudflare Pages でデプロイ可能な状態を維持する
- `npm run build` を通る状態を維持する

---

## 3. 最終的に意図しているMVPまとめ

意図しているMVPは以下:

- モバイル向けの探索型パズルゲーム
- プレイヤーは広めで不規則な安全洞窟から始まる
- 十字キーで移動する
- 4方向の向きを持つ
- 正面の壁を「叩く」ボタンで壊す
- 安全な壁を開くと床になり、マインスイーパー数字が出る
- 地雷入りの壁を壊すと爆発し、ダメージを受け、通行可能な焼け跡になる
- スタート領域の外のどこかに隠されたゴールがある
- プレイヤーは数字を読んで安全なルートを選び、ゴールへ向かう
- Cloudflare Pages 上でURL公開して遊べる構成にする

これが、後続の明示的な指示がない限り、すべての実装作業の目標挙動である。

---

## 4. 概要

### タイトル
Minewalker

### ジャンル
マインスイーパー着想の探索パズルゲーム

### コアアイデア
プレイヤーは、部分的に開いた安全な洞窟の中でキャラクターを操作し、マインスイーパー風の数字を読み、正面の壁を壊し、地雷を避けながら隠されたゴールを探す。

これは従来の盤面全体を解くマインスイーパーではない。
プレイヤーの目的は、盤面全体を処理することではなく、**前へ進むための安全なルートを推理すること**である。

---

## 5. MVPの範囲

MVPに含めるもの:

- 操作可能なプレイヤーキャラクター
- 画面上の十字キーによる4方向移動
- 4方向の向き
- プレイヤー正面の壁を壊す「叩く」ボタン
- 安全な壁 / 地雷入りの壁の判定
- マインスイーパー型の周囲地雷数表示
- HPと地雷ダメージ
- 地雷後の焼け跡マス
- 隠しゴール
- 勝利 / 敗北状態
- モバイル縦画面前提のUI

MVPに含めないもの:

- Ore
- 回復アイテム
- ピッケル強化
- 敵
- マルチプレイ
- タイマー
- flagモード
- 0の自動展開
- 複雑なRPGシステム
- ストーリーシステム

---

## 6. 対応盤面サイズ

対応する盤面サイズ:

- 中級: 16 x 16、地雷40
- 上級: 30 x 16、地雷99

補足:
- マス数は固定
- 表示スケールのみ画面サイズに応じて調整してよい
- 明示的な指示がない限り盤面サイズを変えない

---

## 7. マスの種類

各セルは次のいずれかである:

- 壁
- 開いた床
- 焼け跡マス
- スタートマス
- 隠しゴールマス
- 表示済みゴールマス

### 7.1 壁
- 閉じた / 未開封状態
- 通行不可
- プレイヤー正面にある時だけ壊せる

### 7.2 開いた床
- 通行可能
- 標準的なマインスイーパーのルールで周囲地雷数を表示する
- 周囲地雷数が0なら数字を省略してよい

### 7.3 焼け跡マス
- 地雷入りの壁を叩いた結果できる
- 通行可能
- 通常床と見た目で区別する
- 暗い焼け跡色を使う
- 周囲地雷数を表示する
- 周囲地雷数が0なら数字を省略してよい

### 7.4 スタートマス
- 安全
- 初期安全地帯の中にある
- プレイヤーの開始位置

### 7.5 隠しゴールマス
- 最初から存在する
- 初期状態では閉じたマス / 隠れた目的地として扱う
- 開くまで見えない

### 7.6 表示済みゴールマス
- 隠しゴールマスが開いた時に出現する
- 床や焼け跡マスとは異なる見た目
- 周囲地雷数は表示しない
- このマスに到達するとクリア

---

## 8. 数字ルール

周囲地雷数は標準的なマインスイーパーのルールに従う。

### 8.1 カウントルール
開いた床または焼け跡マスについて:
- 周囲8マスの地雷数を数える

### 8.2 表示ルール
- 1以上なら表示する
- 0は空表示でよい
- ゴールマスには数字を表示しない

### 8.3 0の扱い
- MVPでは0の自動展開をしない
- 0のマスを開いても、そのマスだけが開く

---

## 9. プレイヤールール

### 9.1 プレイヤーエンティティ
プレイヤーは盤面上の1キャラクターを操作する。

### 9.2 向き
プレイヤーの向きは4方向のみ:
- 上
- 下
- 左
- 右

この向きによって、叩く対象の壁が決まる。

---

## 10. 操作

### 10.1 移動
移動は画面上の十字キーを使う。

ルール:
- 各入力で1マス移動を試みる
- 移動は即時
- MVPでは移動アニメは不要
- 移動先が通行可能なら移動する
- 移動先が塞がれていればその場に留まる

### 10.2 向き変更
- 方向入力をすると、プレイヤーの向きがその方向に変わる
- 移動できなくても向きは変わる

これにより、壁の前で向きだけ変えてから叩ける。

### 10.3 叩くボタン
画面上に専用の「叩く」ボタンを置く。

ルール:
- 対象は必ず1マス
- プレイヤー正面のマスを対象にする
- 4方向のみ
- 斜めは不可

### 10.4 長押し連打
叩くボタンは押しっぱなし連打に対応する。

ルール:
- 0.25秒ごとに繰り返す
- この間隔は定数として調整可能にする

---

## 11. 壁破壊結果

プレイヤーが正面の壁を叩いたとき:

### 11.1 安全だった場合
- 開いた床になる
- 周囲地雷数を表示する
- 通行可能になる

### 11.2 地雷だった場合
- 爆発する
- プレイヤーのHPが1減る
- そのマスは焼け跡マスになる
- 焼け跡マスは通行可能
- 焼け跡マスにも周囲地雷数を表示する
- プレイヤーはノックバックを受ける

### 11.3 ノックバック
MVPでのノックバック挙動:
- プレイヤーは壊したマスへ前進しない
- 元の位置に留まる
- 見た目や感覚として、反動で前進に失敗したように感じられればよい

---

## 12. HPと敗北

### 12.1 初期HP
- プレイヤーの初期HPは3

### 12.2 ダメージ
- 地雷入りの壁を叩くと1ダメージ

### 12.3 ゲームオーバー
- HPが0になったら敗北

---

## 13. ゴールルール

### 13.1 隠しゴール
- ゴールは最初から隠れている
- 盤面の中に隠しゴールマスとして埋め込まれている

### 13.2 出現条件
- そのマスを開いた時だけゴールが見える

### 13.3 ゴールマス表示
- ゴールマスには周囲地雷数を表示しない

### 13.4 勝利条件
- 表示されたゴールマスに移動するとクリア

---

## 14. 初期安全地帯

ゲームは、比較的広く、最初から開いている安全な洞窟から始まる。

### 14.1 安全地帯の要件
- 1つの連結領域であること
- 飛び地がないこと
- ランダム形状であること
- 固定長方形ではないこと
- 細い通路も許可する
- 形は不規則でよい
- 分岐数は1〜4
- 地雷なし
- スタート位置を含むこと

### 14.2 安全地帯の面積
目標面積:
- 81マス前後
- 許容範囲は66〜96マス

### 14.3 安全地帯生成の意図
初期安全地帯は、次のように感じられるべき:
- スタート用の洞窟
- 最初から数字を読んで考えられる程度に広い
- きっちりした長方形ではなく不規則
- 開始直後から遊びやすい

---

## 15. 地雷配置

### 15.1 地雷総数
選択した盤面サイズに応じて、標準的なマインスイーパーと同じ地雷数を使う:
- 16x16 -> 40
- 30x16 -> 99

### 15.2 配置制約
- 地雷は初期安全地帯の中に置かない
- 初期安全地帯の外の壁マスに置く

### 15.3 密度意図
選択した難易度に対して、標準的なマインスイーパーに近い密度にする。

---

## 16. ゴール配置

### 16.1 配置領域
ゴールは:
- 初期安全地帯の外
- スタートから十分遠い位置
に置く

### 16.2 最低距離ルール
スタートを中心に、18x18相当の近傍禁止領域を使う。

MVPでの実用的な解釈:
- スタート周辺にゴール配置禁止領域を定義する
- ゴールをその近距離領域内に置かない

### 16.3 意図
ゴールがスタート近くに出すぎないようにする。

---

## 17. UIレイアウト

このゲームはモバイルファーストで、特に縦持ちスマホを前提にする。

### 17.1 上部HUD
最小限に保つ。
含めるもの:
- HP
- ヘルプボタン
- 必要ならリスタートボタン

上部を情報で埋めすぎないこと。

### 17.2 メイン盤面領域
表示するもの:
- 盤面
- プレイヤー
- 開いた床の数字
- 焼け跡マスの数字
- 出現後のゴール

盤面が視覚的な主役であること。

### 17.3 下部操作領域
含めるもの:
- 十字キー
- 叩くボタン
- リスタートボタン（上部にない場合）

### 17.4 ヘルプモーダル
説明は常時表示せず、`?` ボタンで表示する。

推奨ヘルプ内容:
- 十字キーで移動
- 移動できなくても向きは変わる
- 叩くと正面の壁を壊す
- 数字は周囲地雷数
- 地雷はダメージになる
- 焼け跡マスは通れる
- ゴールは開くまで見えない

---

## 18. 推奨モジュール境界

推奨する責務分割:

- 盤面設定
- 盤面生成
- 安全地帯生成
- 地雷配置
- ゴール配置
- タイル状態モデル
- プレイヤー移動 / 向き
- 叩く処理
- HP / 敗北判定
- ゴール出現 / 勝利判定
- モバイル入力UI
- HUD / ヘルプモーダル
- 盤面スケーリング / モバイルレイアウト