# Phase 0 実施記録（現状把握と固定）

## 1. 着手前チェック（AGENTS.md再確認）
- [x] MVP範囲外を追加しない
- [x] 盤面サイズ（16x16/40, 30x16/99）を変更しない
- [x] モバイル縦画面優先のUI方針を守る
- [x] `npm run build` を通す

## 2. `GameScene` 責務分類
対象: `src/game/GameScene.ts`

### 盤面生成
- `newRun`: ラン状態初期化、グリッド生成、隣接地雷数計算、セル描画オブジェクト再生成。
- `buildGrid`: `GRID_W x GRID_H` の初期セルを作り、ランダム地雷を配置。
- `computeAdjacency`: 全セルの周囲8方向地雷数を計算。
- `ensureFirstRevealIsSafe`: 初手地雷回避のため、必要時に地雷と安全セルを入れ替え再計算。
- `neighbors`, `inRange`: 近傍・境界判定ユーティリティ。

### 入力
- `bindCellPointer`: タップ/長押しのポインタイベント処理を定義。
- `onCellTap`: タップ時の分岐（chord / flag / reveal）。
- `toggleFlag`: 旗ON/OFF切替。
- `tryChord`: 数字セルの同時開放処理。
- `revealAction`: 開封起点処理（初手安全化、タイマー開始、開封）。

### 描画
- `computeLayout`: セーフエリア考慮レイアウト算出。
- `drawFrames`: 上下パネル・盤面枠の描画。
- `addTopUi`, `addBottomUi`, `createHelpModal`, `makeButton`: HUD/操作/UI部品生成。
- `redrawAll`, `redrawCell`: セル見た目更新。
- `refreshUi`: 地雷残数・タイマー・flag状態表示更新。

### 勝敗遷移
- `revealCell`: 地雷開封時の敗北遷移、通常開封、0連鎖開封呼び出し。
- `expandZeroes`: 0連鎖開封（BFS）。
- `checkEndState`: 全安全セル開放時の勝利遷移、終了時タイマー停止。
- `revealAllMines`: 敗北時の地雷可視化。
- `startTimer`, `formatTime`: 計時制御。

## 3. 既存定数抽出と未定数化項目

### 既存定数（コード上で宣言済み）
- 盤面: `GRID_W = 10`, `GRID_H = 14`, `MINE_COUNT = 24`
- 入力: `LONG_PRESS_MS = 420`, `TAP_MOVE_TOLERANCE = 14`
- 見た目: `NUMBER_COLORS`, `CELL_SYMBOL`

### 未定数化項目（今後フェーズで外出し候補）
- レイアウト: `horizontalPadding(8)`, `gap(6)`, `topPanelH(58)`, `bottomPanelH(52)`
- UI寸法: 下部ボタン高さ `32`、モーダル高さ上限 `270`、各種余白 `10/12/20/24/38` など
- 色コード: フレーム/ボタン/セル背景の16進値が分散
- 文字サイズ: `'15px'`, `'13px'`, `'12px'`, `'19px'`, `'16px'` が分散
- タイマー刻み: `startTimer` の `delay: 1000`

## 4. MVP範囲外要素の混入確認
現行実装は「通常マインスイーパー」に寄っており、Minewalker MVP要件と差分あり。

## 4.1 MVP範囲チェック（明確化）

### MVPに含める要素（AGENTS.md基準）
- [ ] プレイヤーキャラクター（盤面上の1体）
- [ ] 十字キーによる4方向移動
- [ ] 4方向の向き管理（移動失敗時も向き更新）
- [ ] 正面1マスを対象にした「叩く」ボタン
- [ ] 叩き結果の分岐（安全壁→床 / 地雷壁→爆発+焼け跡）
- [ ] 周囲8マス地雷数表示（床・焼け跡）
- [ ] HP管理（初期HP=3、地雷で-1）
- [ ] 敗北状態（HP=0）
- [ ] 隠しゴール（開くまで不可視）
- [ ] 勝利状態（可視化後ゴール到達）
- [ ] モバイル縦画面UI（上HUD/盤面/下操作）

判定:
- 現行コードは上記を **ほぼ未実装**（現状は「マインスイーパー開封ゲーム」）
- このためフェーズ1以降は、上記11項目を実装完了条件として追跡する

### MVPに含めない要素（AGENTS.md基準）
- [x] flagモード（現行に存在するため削除対象）
- [x] 0自動展開（現行に存在するため削除対象）
- [x] タイマー（現行に存在するため削除対象）
- [x] 複雑なRPG要素（現行未混入）
- [x] 敵/マルチプレイ/ストーリー（現行未混入）

判定:
- 現行コードには MVP非対象機能が混在しているため、Phase 3〜4 で段階削除・置換する

### 混入（削除・置換候補）
- `flag mode`（MVP非対象）
- `chord`（MVP非対象）
- 0自動展開 `expandZeroes`（MVP非対象）
- クリア条件「安全マス全開放」（Minewalkerは隠しゴール到達で勝利）
- 初手安全化ロジック（Minewalkerは「壁を叩く」探索前提で設計が異なる）

### 仕様不足として検討が必要な点
- なし（Phase 0 時点では AGENTS.md の更新提案は不要）

## 5. 変更対象ファイル一覧（固定）
- `src/game/GameScene.ts`: 単一責務化のため、暫定的にゲームループ中核（盤面生成/入力/描画/勝敗）を分解対象として扱う。
- `src/main.ts`: Scene構成変更時のエントリ調整対象。
- `src/game/*`（新規分割予定）: 盤面設定・タイル状態・入力・判定ロジックを段階分離する受け皿。

## 6. フェーズ完了記録
- 実装内容: 現状コードを責務分類し、定数抽出とMVP非対象要素を洗い出した。
- 未解決課題: 実コードはまだMinewalker仕様へ未移行（次フェーズで段階対応）。
- AGENTS.md更新要否: 現時点では不要。
